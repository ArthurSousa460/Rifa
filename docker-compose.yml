version: "3.8"

services:
  db: 
    image: mysql:8.0
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: cisco123
      MYSQL_DATABASE: db_rifa
      MYSQL_USER: admin
      MYSQL_PASSWORD: cisco123

    ports:
      - "3306:3306"
    
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:

